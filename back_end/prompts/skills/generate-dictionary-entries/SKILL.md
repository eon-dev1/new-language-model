Find the 15 most common words not yet in the dictionary using get_words_not_in_dictionary for the current language. Each result includes a first_seen location. For each word, use get_parallel_verses at that location to see the word in context alongside the English source text. From the context, infer the definition and part of speech. If a single verse is ambiguous, you may use get_word_index to find additional occurrences. Then save all entries using upsert_dictionary_entries.
